"""Random number"""
import random # 
random_number = random.random() # Generates a random float between 0 and 1
random_number = random.randint(1, 10) # Generates a random integer between 1 and 10
random_number = random.uniform(1, 10) # Generates a random float between 1 and 10
random_number = random.randrange(1, 10) # Generates a random number between 1 and 9
my_list = [1, 2, 3, 4, 5]
random_element = random.choice(my_list) # take a number from my list
""""""
